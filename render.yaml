# Final clean render.yaml - ready for deployment
services:
  - type: web
    name: whatsappbot-backend
    env: java
    buildCommand: mvnw clean install
    startCommand: java -jar target/*.jar
    region: singapore
    plan: free


